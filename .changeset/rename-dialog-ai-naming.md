---
"@open-codesign/desktop": minor
---

Add an "AI naming" action to the rename dialog: it summarizes the session's recent conversation (via the per-design chat log) together with the current name through the existing configured model, fills the suggestion into the input for the user to edit before saving, and falls back to the design's preview text when the session has no chat rows.
