---
"@open-codesign/desktop": minor
---

Move the "new session" entry from design cards and hub context menus into a "New chat" button at the top of the chat sidebar, matching the Codex-style mental model of starting another session in place. The new session still shares the same workspace folder and inherits the design brief; the previous session stays available in the sidebar. Continuation names no longer stack the "— continued" suffix across repeated sessions.
