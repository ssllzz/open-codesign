---
"@open-codesign/desktop": minor
---

Add "New session" action on design cards: creates a continuation design that shares the same workspace folder (no file copy) and inherits the design brief and run preferences, so long-lived designs can keep iterating without dragging an ever-growing chat history. Concurrent generations on a shared workspace remain mutually excluded.
